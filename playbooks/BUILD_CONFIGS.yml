---
- name: Apply build role
  gather_facts: no
  hosts: "{{ kit_number }}"
  tasks:
  - set_fact:
      start_time: "{{ lookup('pipe', 'date +%Y%m%d%H%M%S') }}"
    delegate_to: localhost
    delegate_facts: true
  - name: Create directory to store configuration files
    file: path="{{ playbook_dir }}/{{ hostvars['localhost'].start_time }}" state=directory
    delegate_to: localhost
  - name: output unknown variable names
    debug:
      msg:
      - "The local host thing is {{ hostvars['localhost'] }}"
      - "The playbook directory referenced above is {{ playbook_dir }}"
